#!/bin/sh
xhost +local:docker

docker run -it --rm \
  -e DISPLAY=$DISPLAY \
  -v /tmp/.X11-unix:/tmp/.X11-unix \
  -v /usr/share/fonts:/usr/share/fonts \
  -v $HOME:$HOME \
  --device /dev/dri \
  --group-add video \
  kicad8 "$@"
